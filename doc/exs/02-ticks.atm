spawn {
    watching :done {
        par {
            loop on :tick {
                print "tick A"  ;; always awakes first
            }
        } with {
            loop on :tick {
                print "tick B"  ;; always awakes last
            }
        }
    }
    print "done"
}
emit(:tick)     ;; --> "tick A", "tick B"
emit(:tick)     ;; --> "tick A", "tick B"
emit(:done)     ;; --> "done"
print "end"     ;; --> "end"
