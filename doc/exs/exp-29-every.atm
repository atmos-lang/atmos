require "atmos.env.clock"

spawn {
    loop on 1s {
        print("1 more second has elapsed")
    }
}

spawn {
    loop it on :X {    ;; (`emit :X [v=10]`)
        print(it.v)    ;; --> 10
    }
}

emit <-- :X [v=10]
await(false)
