# 260616-gte : complete deep comparisons `=>=` `=<=`

## Context

Deep comparison currently has only equality:

- `===`  deep equal      → `X.eq`
- `=!=`  deep not-equal  → `(not X.eq)`

Complete the family with deep ordered comparisons:

- `=>=`  deep greater-or-equal  → `X.gte`
- `=<=`  deep less-or-equal     → `X.lte`

(assumed to exist in the lua-atmos `atmos.x` module, like `X.eq`;
bound as the uppercase global `X` in `src/exec.lua:23`)

These are the structural variants of the plain numeric `>=` / `<=`,
which already compile straight to Lua operators.

## Mapping

| op    | new emit          |
| ----- | ----------------- |
| `=>=` | `X.gte(a,b)`      |
| `=<=` | `X.lte(a,b)`      |

## Files

| file          | place              | change                              |
| ------------- | ------------------ | ----------------------------------- |
| src/global.lua | `OPS.vs` (46-47)   | add `'=>='`, `'=<='`                 |
| src/global.lua | `OPS.bins` (61)    | add `'=>='`, `'=<='`                 |
| src/coder.lua  | `bin` block (81+)  | add 2 branches → `X.gte` / `X.lte`   |

## Steps

1. `src/global.lua`: add `=>=` `=<=` to `OPS.vs` and `OPS.bins`.
2. `src/coder.lua`: add two `elseif` branches beside `===`/`=!=`.
3. No lexer change: `=`,`>`,`<` are in `OPS.cs`; the operator reader
   backtracks against `OPS.vs` (`src/lexer.lua:122-131`), so listing
   them in `OPS.vs` is enough to tokenize.
4. No parser change: all binaries share one flat level via
   `contains(OPS.bins, ...)` (`src/parser.lua:366`).

## Risks / open

- Naming: `X.gte` / `X.lte` assumed; confirm against lua-atmos
  (could be `X.ge` / `X.le`).
- Lexing ambiguity: `=>` (if-expr arrow) and `>=` already exist.
  The greedy reader takes `=>=` only when the 3 chars are contiguous;
  `if c => x` keeps `=>` (space stops the run).
  Verify no source uses `=>=` meaning `=> =`.
- Semantics live in lua-atmos; this change is purely lex/parse/emit.

## Status

- [ ] global.lua OPS.vs + OPS.bins
- [ ] coder.lua 2 branches
- [ ] confirm `X.gte` / `X.lte` names
